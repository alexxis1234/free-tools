export default async file => { if(!file) throw Error("Select an image first"); const url=URL.createObjectURL(file); const img=new Image(); await new Promise((res,rej)=>{img.onload=res;img.onerror=()=>rej(Error("Could not read image"));img.src=url}); const c=document.createElement("canvas"); c.width=img.naturalWidth;c.height=img.naturalHeight; const ctx=c.getContext("2d"); if("png-to-webp"==="webp-to-jpg"){ctx.fillStyle="#fff";ctx.fillRect(0,0,c.width,c.height)} ctx.drawImage(img,0,0); const mime="image/webp"; const blob=await new Promise(r=>c.toBlob(r,mime,.92)); URL.revokeObjectURL(url); if(!blob) throw Error("Conversion failed"); return blob; };
