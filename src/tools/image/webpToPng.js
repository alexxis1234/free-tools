export default file=>new Promise((resolve,reject)=>{const img=new Image();const url=URL.createObjectURL(file);img.onload=()=>{const c=document.createElement("canvas");c.width=img.naturalWidth;c.height=img.naturalHeight;c.getContext("2d").drawImage(img,0,0);c.toBlob(b=>{URL.revokeObjectURL(url);b?resolve(b):reject(Error("Conversion failed"))},"image/png")};img.onerror=()=>{URL.revokeObjectURL(url);reject(Error("Invalid image"))};img.src=url});
