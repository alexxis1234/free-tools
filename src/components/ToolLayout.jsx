import ToolCard from "./ToolCard"; export default function ToolLayout({tool,related,children}){return <><div className="breadcrumbs"><a href="/">Home</a> / <a href={`/${tool.category}/`}>{tool.category}</a> / {tool.name}</div><section className="hero"><h1>{tool.name}</h1><p>{tool.description}</p></section>{children}<section className="info"><h2>About this tool</h2><p>{tool.description} This tool runs in your browser and does not require an account.</p><h2>Related tools</h2><div className="related">{related.map(x=><ToolCard key={x.slug} tool={x}/>)}</div></section></>}